<% provide(:title, 'All Entries') %>

<div class="row">
	<aside class="col-md-3">
 		<!-- <%= link_to image_tag("c.png", alt: "Rails logo", class: "img-responsive") ,root_path %> -->
	</aside>
	<div class="col-md-9 ">
		<ul class="microposts">
  			<h3>Entries (<%= @entry.count %>)</h3>
  			<%= render @entry %>
  			<%= will_paginate @entry %>
		</ul>
	</div>
</div>
